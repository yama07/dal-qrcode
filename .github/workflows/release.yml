name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Zip extensions
        run: |
          npm run zip
          npm run zip:firefox

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          files: |
            .output/dal-qrcode-${{ github.ref_name }}-chrome.zip
            .output/dal-qrcode-${{ github.ref_name }}-firefox.zip
            .output/dal-qrcode-${{ github.ref_name }}-sources.zip
            .output/ThirdPartyNotices.txt
